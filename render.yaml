services:
  - type: web
    name: gnex-tts-bot
    env: python    ✅ This enables runtime.txt to work
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
